# Claude Code context

Read `AGENTS.md` first — it is the canonical agent orientation for this
repository. `CLAUDE.md` exists so Claude-facing tooling reaches the same
instructions; keep both files aligned.
